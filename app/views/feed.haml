- area_class 'feed compact'

- content_for :content do
  %header
    %h1= @feed.title

  - @entries.each do |entry|
    %article
      %h2
        %a{href: R(:feed, @feed, :entry, entry)}= entry.title
      %p!= entry.content

!= extends :news
