- if session[:error]
  #error= session.delete(:error)
%form{action: '/login', method: 'post'}
  %input{name: 'email'}
  %input{type: 'password', name: 'password'}

  %button{type: 'submit'}
    Login

or
%a.ext{href: R(:auth, :google), target: '_self'} Google connect
