
#all.login
  #login
    - if session[:error]
      #error= session.delete(:error)

    %p.connect
      %a.button.black{href: R(:auth, :google), target: '_self'} Google Connect

    %p.or
      %span or

    %form{action: '/login', method: 'post'}
      %p
        %input{name: 'email', placeholder: "Google email"}
      %p
        %input{type: 'password', name: 'password', placeholder: "Password"}

      %p.submit
        %button{type: 'submit'}
          Login


