-# This is going to be a JS model... this is not presentational.
-# See NN.FeedView (JS).

%article
  %a.title{href: R(:feed, entry.feed, :entry, entry)}!= entry.title
  - if entry.image?
    %img.image{src: entry.image_url}
  %div.summary= entry.summary
  %div.content= entry.content
